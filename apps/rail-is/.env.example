# Rail İş (Diyar B) native dron — apps/rail-is
# Amiral .env.local kopyalanmaz. service_role yoktur.
# cp .env.example .env.local  sonra laboratuvar değerleriyle doldur.

# Amiral kökeni (kuyruk yok). Örnek: http://192.168.1.5:3000
EXPO_PUBLIC_RAIL_API_BASE=""

# Supabase Auth — anon / publishable. service_role YASAK.
EXPO_PUBLIC_SUPABASE_URL=""
EXPO_PUBLIC_SUPABASE_ANON_KEY=""
