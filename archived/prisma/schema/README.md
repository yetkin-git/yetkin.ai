# archived/prisma/schema — donmuş 23 model (tarih)

Bu dosyalar **Prisma generate yolunda değildir**. Canlı şema `prisma/schema/` yalnız kernel + academy + career + freelancer taşır.

P3 DROP migrasyonu: `prisma/migrations/20260822010000_drop_frozen_room_tables`
(23× `DROP TABLE IF EXISTS … CASCADE`). Hosted apply disk planı SQL gövdesini mühürler.

Geri açmak ürün kararıdır; bu klasörü `prisma/schema/` altına kopyalamak yetmez.
