import { FrozenSquadNotice } from "@/components/freelancer/frozen-satellite";

/** Squad UI bu fazda dondurulmuştur. */
export function SquadCreateButton(_props?: { className?: string }) {
  return <FrozenSquadNotice />;
}
