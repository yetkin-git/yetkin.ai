node_modules/
.pnp
.pnp.js

.env
.env.local
.env.*.local
.env.development
.env.test
.env.production
.env.museum-backup.local
!.env.example

# Kimlik / anahtar kalkanı — verify:no-secrets ile birlikte.
*.pem
*.p12
*.pfx
*.p8
*.jks
*.mobileprovision
credentials.json
serviceAccount.json

.next/
out/
dist/
coverage/
*.tsbuildinfo

# Prisma çıktısı yalnız kökte. Dron v1 istemcisi (apps/rail-is/src/generated) git'tedir.
/generated/
/generated/prisma/

.cursor/
.vercel/
.vscode/
.idea/
*.swp
*.swo
*~

.DS_Store
Thumbs.db
Desktop.ini

logs/
*.log
npm-debug.log*

test-results/
playwright-report/

# Saha probe / operatör kalıntısı — kökte durmaz (01 tespit hijyeni).
.tmp/
temp/
tmp/
.cache/
.eslintcache
gitstatus.txt

# Müze — ilham ve yasak listesi (OPS). Rail git geçmişine girmez.
# Nested repo; `git add` ve ajan taraması dışındadır. Kod silinmez.
yetkin_muze/

# Native / Expo
apps/*/.expo/
apps/*/dist/
apps/*/web-build/
apps/*/expo-env.d.ts
apps/*/.kotlin/

